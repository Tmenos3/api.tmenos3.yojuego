describe('MatchRoutes', () => {
  var server;

  beforeEach(function() {
    // server = require('mongodb');
    // mongodb.MongoClient.connect = mockedConnect(false, {});
  });

  afterEach(function() {
    // mongodb = null;
  });
});
